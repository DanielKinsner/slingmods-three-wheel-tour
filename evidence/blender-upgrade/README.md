# Blender detail review — September 10

This folder contains actual rendered art and isolated headless gameplay evidence, not generated concept screenshots. The runtime bundle and three shipped GLBs are hashed in `tested-build.json`. Run `npm test`, `npm run build`, `python scripts/validate-hero.py`, `python scripts/validate-harbor.py`, serve the build on 4175, then run `node scripts/headless-blender-upgrade.mjs` and `node scripts/headless-harbor-fallback.mjs`.

- `before-vehicle.png`, `before-rear.png`, `before-assets.json`: neutral Blender views and asset inventory from the starting production checkout, before changes. Prior actual Harbor gameplay views remain in `../playground/final-harbor-*.png`; camera timing/paint differ, so these are visual references rather than a controlled performance comparison.
- `../../artwork/vehicle/three-quarter.png`, `rear.png`, `front.png`, `side.png`, `cockpit.png`: updated full-model neutral Blender renders. `wheel-detail.png` and `rear-suspension-detail.png` expose the actual new mesh detail. Five original material maps and the editable port master live in `../../artwork/harbor/`.
- `garage-front.png`, `garage-rear.png`, `garage-rgb.png`: actual owner garage, including retained blue paint, silver finish and purple underglow. `final-harbor-{warehouse,covered,tanks,cranes,waterfront,cargo,night}.png`: actual virtual-gamepad drive through the new models. `harbor-cockpit.png`, `six-car-race.png`, `harbor-reloaded.png`, `mobile-emulation.png`: additional runtime views.
- `harbor-blender-drive-silent.mp4`: unretimed 35-second excerpt from the final 960×600 headless gameplay recording; no audio track. Raw source stays local under ignored `raw/`. This is browser footage, not a desktop recording or audio audition.
- `final-visuals.json`: final 1440×900 WebGL run; all model requests 200, eleven links, day/night drive, five opponents in an existing race, cache reload, save equality, reduced-only vehicle at mobile boot, zero game errors/warnings. Frame profiles accumulate active racing frames within the context; the night snapshot includes earlier day frames. 4,405 frames through final reload: p50 16.7 ms, p95 16.8, p99 16.8, max 50.1; nine over 33.34 ms. Renderer counters include multiple passes and must not be equated with unique asset triangles. No GPU timers or physical phone/controller validation. Default renderer boot selected WebGL 2, not WebGPU.
- `fallback.json`, `missing-kit-fallback.png`: intentional failed Harbor model request; the original procedural route still renders and drives. Expected network error/warning is distinct from a game exception.
- `first-harness-failure.json/png`: initial dev-server capture failed because its module-script selector matched both Vite client and application. No game failure. The final harness uses the built preview server. `first-playable-pass.json`: successful intermediate run before apron seating/material sharing and the corrected diagnostic label for mobile fallback quality.

No changes to engine/SFX/voice/music files or their levels. All paid services and desktop/browser profiles were untouched. The standalone Blender turntable frames from earlier delivery were not regenerated in this pass and remain historical.
